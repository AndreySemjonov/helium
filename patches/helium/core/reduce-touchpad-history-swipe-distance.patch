--- a/chrome/browser/helium_flag_choices.h
+++ b/chrome/browser/helium_flag_choices.h
@@ -34,6 +34,18 @@ namespace helium {

   constexpr const char kMiddleClickAutoscrollCommandLine[] = "middle-click-autoscroll";

+  constexpr const char kTouchpadHistorySwipeThresholdCommandLine[] =
+      "touchpad-overscroll-history-navigation-threshold";
+  constexpr const FeatureEntry::Choice kTouchpadHistorySwipeThresholdChoices[] = {
+    {flags_ui::kGenericExperimentChoiceDefault, "", ""},
+    {"Very very short (8%)", kTouchpadHistorySwipeThresholdCommandLine, "0.08"},
+    {"Very short (12%)", kTouchpadHistorySwipeThresholdCommandLine, "0.12"},
+    {"Short (18%)", kTouchpadHistorySwipeThresholdCommandLine, "0.18"},
+    {"Medium (24%)", kTouchpadHistorySwipeThresholdCommandLine, "0.24"},
+    {"Chromium default (30%)", kTouchpadHistorySwipeThresholdCommandLine, "0.30"},
+    {"Long (40%)", kTouchpadHistorySwipeThresholdCommandLine, "0.40"},
+  };
+
 }  // namespace helium

 #endif  /* CHROME_BROWSER_HELIUM_FLAG_CHOICES_H_ */
--- a/chrome/browser/helium_flag_entries.h
+++ b/chrome/browser/helium_flag_entries.h
@@ -33,5 +33,9 @@
     {helium::kMiddleClickAutoscrollCommandLine,
      "Middle Click Autoscroll",
      "Enables autoscroll on middle click. Helium flag, Chromium feature.",
      kOsDesktop, FEATURE_VALUE_TYPE(blink::features::kHeliumMiddleClickAutoscroll)},
+    {helium::kTouchpadHistorySwipeThresholdCommandLine,
+     "Touchpad history swipe distance",
+     "Configures how far a touchpad history navigation swipe must travel before it completes. Lower values trigger back/forward navigation with a shorter swipe. Helium flag.",
+     kOsDesktop, MULTI_VALUE_TYPE(helium::kTouchpadHistorySwipeThresholdChoices)},
 #endif  /* CHROME_BROWSER_HELIUM_FLAG_ENTRIES_H_ */
--- a/content/browser/renderer_host/overscroll_configuration.cc
+++ b/content/browser/renderer_host/overscroll_configuration.cc
@@ -14,6 +14,9 @@ bool g_is_ptr_mode_initialized = false;
 content::OverscrollConfig::PullToRefreshMode g_ptr_mode =
     content::OverscrollConfig::PullToRefreshMode::kDisabled;

+constexpr char kTouchpadHistorySwipeThresholdSwitch[] =
+    "touchpad-overscroll-history-navigation-threshold";
+
 bool g_is_touchpad_overscroll_history_navigation_enabled_initialized = false;
 bool g_touchpad_overscroll_history_navigation_enabled = false;

@@ -22,18 +25,35 @@ bool g_touchpad_overscroll_history_navig
 // overscroll if it is not completed yet.
 int g_max_inertial_events_before_overscroll_cancellation_in_ms = 300;

+float GetCompleteTouchpadThresholdPercent() {
+  double threshold;
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch(
+          kTouchpadHistorySwipeThresholdSwitch) &&
+      base::StringToDouble(
+          base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+              kTouchpadHistorySwipeThresholdSwitch),
+          &threshold) &&
+      threshold > 0.0 && threshold <= 1.0) {
+    return static_cast<float>(threshold);
+  }
+  return 0.18f;
+}
+
 }  // namespace

 namespace content {

-// static
-const float OverscrollConfig::kCompleteTouchpadThresholdPercent = 0.3f;
 const float OverscrollConfig::kCompleteTouchscreenThresholdPercent = 0.25f;

 // static
 const float OverscrollConfig::kStartTouchpadThresholdDips = 60.f;
 const float OverscrollConfig::kStartTouchscreenThresholdDips = 50.f;

+// static
+float OverscrollConfig::CompleteTouchpadThresholdPercent() {
+  return GetCompleteTouchpadThresholdPercent();
+}
+
 // static
 OverscrollConfig::PullToRefreshMode OverscrollConfig::GetPullToRefreshMode() {
   if (g_is_ptr_mode_initialized)
--- a/content/browser/renderer_host/overscroll_controller.cc
+++ b/content/browser/renderer_host/overscroll_controller.cc
@@ -317,7 +317,7 @@ bool OverscrollController::DispatchEvent
   const float ratio = fabs(delta) / std::max(size.width(), size.height());
   const float threshold =
       overscroll_source_ == OverscrollSource::TOUCHPAD
-          ? OverscrollConfig::kCompleteTouchpadThresholdPercent
+          ? OverscrollConfig::CompleteTouchpadThresholdPercent()
           : OverscrollConfig::kCompleteTouchscreenThresholdPercent;
   return ratio >= threshold;
 }
--- a/content/browser/web_contents/aura/gesture_nav_simple.cc
+++ b/content/browser/web_contents/aura/gesture_nav_simple.cc
@@ -573,7 +573,7 @@ void GestureNavSimple::OnOverscrollModeC
       is_touchpad ? OverscrollConfig::kStartTouchpadThresholdDips
                   : OverscrollConfig::kStartTouchscreenThresholdDips;
   const float complete_percent =
-      is_touchpad ? OverscrollConfig::kCompleteTouchpadThresholdPercent
+      is_touchpad ? OverscrollConfig::CompleteTouchpadThresholdPercent()
                   : OverscrollConfig::kCompleteTouchscreenThresholdPercent;

   const gfx::Size size = GetDisplaySize();
--- a/content/public/browser/overscroll_configuration.h
+++ b/content/public/browser/overscroll_configuration.h
@@ -25,7 +25,7 @@ class CONTENT_EXPORT OverscrollConfig {
   };

   // These are percentages of the display size.
-  static const float kCompleteTouchpadThresholdPercent;
+  static float CompleteTouchpadThresholdPercent();
   static const float kCompleteTouchscreenThresholdPercent;

   static const float kStartTouchpadThresholdDips;
